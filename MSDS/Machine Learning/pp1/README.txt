The assignment is written in ipynb format and can be run using the jupyter notebook

The training data should be named as "training_data.txt"

The test data should be named as "test_data.text"

The three files for the 3rd question are named as "pg121.txt.clean", "pg141.txt.clean", "pg1400.txt.clean"

The files should be present in the same directory as the python notebook

No specific commands to be run along with the notebook

